# 変更履歴・検討ログ

このファイルは、Claude（サポートブレイン）との検討・決定事項を時系列で記録するログです。
コード変更そのものではなく「何を・なぜ決めたか」の背景情報として参照してください。

---

## 命名規則（このログとバックアップ運用のルール）

### バックアップ

- 保管場所：プロジェクト直下 `_backups/`（Gitには含めない。`.gitignore` に追加済み）
- フォルダ名：`YYYYMMDD_HHmmss`（例：`20260922_022900`）
  - 元の提案「修正履歴_yyyymmdd_MM:dd:ss」はコロン（:）がWindowsのファイル名に使用できない文字のため、ハイフン・コロンなしの形に変更
- 中身：修正対象ファイルを、`src/components/Admin.tsx` のように元のディレクトリ構造を保ったまま格納
- 運用：Claudeがソースファイルを修正する前に、その時点の対象ファイルを新しいタイムスタンプフォルダにコピーしてから編集する

### 検討ログ（本ファイル）

- 1ファイルに集約し、日時の新しい順ではなく古い順に追記していく（既存の `docs/00〜11` の番号規則に合わせて `13_` を付与）
- 見出し形式：`## YYYY-MM-DD HH:mm 決定事項のタイトル`
- どのセッション（Claude Code / claude.ai どちらでも）でこのアプリに加えた変更・決定であっても、本ファイルに記載する

### ローテーション（ログが増えすぎないようにする運用）

- 本ファイルの決定事項（`## `見出し）が **20件を超えたら**、古い方から10件を `docs/archive_13_変更履歴/` フォルダへ移動する
- アーカイブファイル名は連番（`01.md`, `02.md`, ...）とし、移動した10件をそのまま古い順で集約する
- 移動時、本ファイルの「命名規則」セクションの直後に「## 過去のログ」を作り、アーカイブファイルへのリンクと期間（例：`[01.md](archive_13_変更履歴/01.md)`（2026-05〜2026-09））を一覧表示する
- 移動後も本ファイル・アーカイブファイルとも見出し形式・古い順の並びは変えない

---

## 2026-09-21 00:21 調整さんCSV一括登録機能を実装

- 調整さんの出欠CSV実サンプルを確認したところ、実際の列構成は「名前・出欠記号・コメント」の3列で、当初想定していた5列構成（名前・参加意思・レベル・種別・登録対象フラグ）とは異なることが判明
- 検討の上、以下の方針で実装
  - 名前が既存の`users`（正規部員）と完全一致すれば登録済みとしてスキップ。全角/半角スペースの表記ゆれのみで一致する場合は、誤って別アカウントを作らないよう自動登録せず「要確認」として一覧に保留表示
  - `guestUsers`（既存ゲスト）との重複チェックも追加し、同名なら未登録として扱う
  - 出欠記号が✕（不参加）・△（未定）の場合は新規アカウントを発行しない（◯の人のみゲストとして新規登録、レベルは未経験者固定）
  - 複数日程の候補列がある場合、活動日設定（`activityDate`）の月日と一致する列だけを出欠として採用。活動日未設定時、または一致する列が無い場合はポップアップでエラー通知
  - CSV冒頭の説明文（クォート内改行を含む複数行フィールド）は、既存の単純な行分割パーサーでは壊れるため、「参加者」列を含むヘッダー行を検出し、それより前を読み飛ばす方式で対応
  - エラーメッセージはID一元管理システムを新設せず、ローカル文字列のまま実装（対象が2件のみで過剰設計と判断）
- 実データ（29名分のCSVサンプル）でパースロジックを単体検証し、型チェック・ビルドとも正常を確認
- コミット：`fdfeaad feat: 調整さんCSVから出欠を一括取込みゲスト登録する機能を追加`

## 2026-09-21 00:23 ローカル・リモートのGit分岐を統合

- ローカル`main`（CSV一括登録機能などのコミット3件、2026-05-24時点）と、リモート`origin/main`（Footer機能などのコミット6件、2026-09-20時点）が分岐していたことが判明
- 調査の結果、内容面での対立はなし（リモートの`Update Admin.tsx`コミットに、ローカルのCSV機能と同一内容が既に含まれていた）と確認
- `git merge origin/main`で統合。`Admin.tsx`で軽微なコンフリクトが発生したが、原因は隣接する変更箇所同士の近さによるもので実質的な対立ではなく、こちら側（HEAD）の内容を採用して解消
- コミット：`497a900 merge: origin/main（Footer機能）を統合`

## 2026-09-21 08:36 ログイン・ログアウト時のタブ復帰漏れを修正

- Git分岐統合の過程で、未コミットのまま残っていた`App.tsx`の変更の中に、Footer機能とは無関係な修正（ログイン成立時・ログアウト時に表示タブをホームへリセットする処理）が混在していることを発見
- ユーザーに意図を確認したところ、「オーナーが管理画面からログアウトした後、同じタブで別のメンバーがログインすると、直前のタブ（管理画面）が表示されたままになり、自分でホームへ遷移する必要がある」ための修正と判明
- 意図した修正として採用しコミット
- コミット：`6030295 fix: ログイン・ログアウト時に表示タブをホームへ強制的に戻す`

## 2026-09-22 02:29 バックアップ体制の構築

- 上記の命名規則を確定し、`_backups/20260922_022900/` に主要ソースファイル一式のベーススナップショットを保存
- `.gitignore` に `_backups/` を追加（本番リポジトリを個人的な作業履歴で汚さないため）

## 2026-09-22 02:29 既存実装の確認：調整さんCSV読み込み機能

- `src/components/Admin.tsx` を確認したところ、**「調整さん 出欠一括登録（CSV）」機能がすでに実装済み**であることを確認
  - 活動日設定の日付と一致する出欠列（◯/△/×）をCSVから自動判定
  - 未登録かつ◯（出席）の人のみゲストとして新規登録（レベルは未経験者固定）
  - 既存の正規部員・ゲストと同名の人はスキップ、表記ゆれ（全角/半角スペース違いなど）も検出してスキップ
  - 登録されなかった人は名前と理由付きで画面に一覧表示
- この機能は「外部の調整さんで集めた出欠をこのアプリに取り込む」ためのもの
- 今回新たに依頼された「調整さんと同じ機能を実装したい」は、これとは別に**アプリ内に出欠収集フォーム自体を作る**（ログイン不要、名前・参加意思・コメントを入力、CSV出力できる）という趣旨として計画を進める

## 2026-09-23 ゲストのレベル情報の扱い（A案採用）

- 試合管理機能と調整機能の影響範囲を確認する中で、既存実装のギャップを発見：ゲストの経験者/未経験者レベルは `memberLevels` に一度も書き込まれておらず、マッチング（試合組み）アルゴリズムはゲストを常に未経験者として扱ってしまう
- 対応案を2つ提示し、**A案（ゲストのレベルも `memberLevels` に書き込む）を採用**と決定
  - A案：新しい調整機能で出欠・レベルを登録／更新する際、ゲストのレベルも `memberLevels` に書き込み、正規部員と同様にマッチングへ反映する
  - B案（不採用）：現状維持。ゲストは常に未経験者としてマッチングされる
- 理由：CSV・フォームでせっかく経験者/未経験者を入力してもらっても、B案では試合組みに反映されず入力が無意味になるため

## 2026-09-23 活動終了ロック中の単一日程モードフォームの扱い

- 既存の「活動終了後の編集ロック（`isActivityEnded`）」と、新しい単一日程モードの出欠回答フォームが同時に有効だと、ロック後に新しい出欠がフォーム経由で登録されてしまい、ロックの意味と矛盾する
- **決定：ロック中は単一日程モードのフォーム自体を締め切り扱いにする**（新規回答を受け付けない）

## 2026-09-23 テスト画面（モックアップ）の作成・調整

- 「調整機能」の管理画面（複数候補日の比較マトリクス＋確定ボタン）と、一般ユーザ向けの出欠回答フォームについて、Claudeのアーティファクト機能でクリック可能なモックアップを作成し、要件のすり合わせに使用
- 1回目の叩き台は本番アプリのダークテーマ・カード型UIに寄せて作成したが、ユーザーから「想像していた画面仕様と違う」とのフィードバックを受け、以下の方針に修正
  - デザイン：本番アプリのダークテーマではなく、**外部ツール「調整さん」に近い白・明るい・シンプルな表デザイン**に変更
  - 一般ユーザ画面の構成：ステップ形式の入力フォームではなく、**調整さん本家に近い「表形式の一括入力」**（他の回答者の行が並び、一番下に自分の入力行がある形）に変更
  - レイアウト：PC・モバイル別々のモックアップではなく、**1つの可変幅レイアウト**で両方を兼ねる形とした
- このモックアップはあくまで画面イメージのすり合わせ用であり、アプリ本体のコードは変更していない（サポートブレインとしての「ファイル修正はしない」という制約の範囲内）

## 2026-09-24 調整機能：名前照合ロジックを2段階構成で確定

- 当初「送信後、名前を完全一致（空白正規化）で自動判定し、一致すれば既存アカウントへ無条件に反映する」という案を提示したが、「別人だった場合に困る」との指摘を受け再検討
- 目的の異なる2つの照合を明確に分離する構成に修正して確定
  - **第1段階：イベント内の重複チェック**（要件「名前の重複は基本認めない」に対応）。今回の調整対象（`chouseisanEntries`）内で正規化名（前後空白除去＋全角/半角スペース統一）が既に存在するか確認する
  - **第2段階：正規部員照合**（要件「正規部員として登録済か照合」「登録済なら本人として」「未登録ならゲスト」に対応）。同じ正規化ロジックで`users`（正規部員名簿）と完全一致照合し、一致すれば正規部員としてレベル上書きなしで反映、一致しなければゲストとして扱う（単一日程モードは即時ID発行、複数候補日モードは仮ID発行）
- 第1段階について、当初は「完全一致なら自動的に上書き」としていたが、以下の経緯で確認ポップアップ方式に変更
  1. 「一致したら無条件に上書き」は、別人が同じ名前で回答した場合に他人の回答を破壊してしまう恐れがあると指摘を受けた
  2. 対応として「同姓同名の回答が既にあるため上書きになります。よろしいですか？」というポップアップを出し、「はい」なら既存回答を上書き、「いいえ」なら別人として新規保存する案を提示
  3. この案について、「いいえ」を選んだ場合に同名2件が残ってしまい、その後の会員照合（第2段階）でどちらの行を本人に紐付けるか機械的に判定できなくなる問題を指摘。また「同姓同名の回答が既にある」という情報自体の開示が新たな情報漏えいにならないか論点として提示したが、「一覧を確認すれば分かることなので問題に当たらない」との判断で合意
  4. 「いいえ」を選んだ場合の扱いについて3案（A：区別用の識別子を付けて別行保存し、管理画面に「同姓同名あり・要確認」として表示、最終判断は管理者が手動で行う／B：機械的に先着1件のみ正規部員に紐付け、2件目はゲスト扱いに固定／C：「いいえ」の選択肢自体を用意せず常に上書きに統一）を提示し、**A案を採用**
  5. 採用理由：同姓同名は実際にはほとんど発生せず、発生しても漢字表記の違い等ですぐ判別できるため、稀に発生した場合のみ管理者が手動で解決すれば十分という判断（「基本認めない」の「基本」は同姓同名が存在しない前提であり、存在する場合は例外として認めるほかない、という整理）

## 2026-09-24 調整機能：送信前の内容確認ステップを追加（採用）

- 誤字・表記ゆれ（スペースの有無等）により、本人のつもりが正規化ロジックで別回答として扱われてしまう事故を防ぐため、送信前に「この内容で送信します：名前／出欠／コメント」という確認表示を挟むことを提案し、採用が決定
- 名前照合ロジック（第1段階・第2段階）を機械的な正規化処理だけに頼らず、入力段階での事故防止も合わせて行う位置づけ

## 2026-09-24 調整機能：なりすまし対策は「対策なし＋事後の検知是正」で確定

- 現状のアプリは、ログイン中のユーザーが自分の名前（`authUser.name`）の出欠しか変更できない設計（`Home.tsx`の`handleAttendance`は常に自分の名前で送信）だが、新しい調整機能のフォームは任意の名前で送信できてしまうため、他人の名前を騙って出欠・レベル希望・コメントを書き換えられるリスクが新たに生まれることを指摘・共有
- 3つの選択肢を提示し検討
  1. **対策なし＋ログ記録のみ（採用）**：能動的な本人確認は行わず、`chouseisanEntries`に「いつ・どの内容で送信されたか」のログのみ残す。正規部員は後でログインした際に、Home画面の出欠ボタンで自分の出欠を訂正できることをセーフティネットとする
  2. 初回回答時に簡易PIN（4桁等）を設定させ、次回同名で編集する際にPIN一致を求める（調整さん本家の「回答保護」に近い発想）
  3. 正規部員だけは別途ログイン画面経由でも回答できるようにし、ログイン不要の入口はゲスト専用に限定する
- **選択肢1を採用**。理由：この機能の核心的価値は「誰でも気軽に、ログイン不要で」参加できることであり、PIN発行（選択肢2）や部分ログイン（選択肢3）を足すとその価値を自ら削ることになる。実害（悪意あるなりすまし）が実際に起きた前例がない段階では、検知・是正型の運用で十分と判断した
- 参考：外部ツール「調整さん」本家も、名前だけでの自動本人識別の仕組みは持っておらず、識別が必要な場面（回答保護機能）ではログインアカウントやブラウザ単位の情報に頼っている。名前の完全一致だけで本人と機械的に断定しないという今回の方針は、本家の設計思想とも整合する

## 2026-09-24 調整機能：管理画面の配置先を確定（案A採用）

- 候補日マトリクス・確定ボタンを持つ管理画面の配置について2案を検討
  - **案A（採用）**：既存の⚙️管理タブの中に新セクション「日程調整」として追加する（既存の「調整さん 出欠一括登録（CSV）」セクションと並べる）
  - 案B（不採用）：新しい専用タブ（例：🗓️調整）を追加する
- 採用理由：管理者向け機能はすべて⚙️管理タブに集約されているという既存の構造に沿っており、タブ数を増やさずに済むため

## 2026-09-24 調整機能：一般ユーザ回答フォームの配置先を確定

- 当初、未ログイン状態でフォームを表示する手段として「URLクエリパラメータ（例：`?schedule=1`）を判定し、ログイン画面より手前で分岐する」案を提示した（本家調整さんの「専用URLを都度共有する」運用に近い発想）
- ユーザーから「ログイン画面（`Login.tsx`）自体にフォームを配置できないか」との提案があり、再検討の結果こちらを採用
- **決定：ログイン画面に「出欠を回答する（ログイン不要）」等のボタンを新設し、押すとログインフォームの代わりに回答フォームを表示する構成とする**。URLクエリパラメータでの分岐案は不採用
- 採用理由：ログイン画面には既に「ゲストとして入場する（閲覧のみ）」というログイン不要の導線が存在する。そこに並べてボタンを設置すれば、全部員が最初にたどり着くこの画面だけで完結し、LINE等で専用リンクを別途配布する運用が不要になる

## 2026-09-24 既存CSV系一括登録機能の要否を整理

- 新しい調整機能が想定通りに機能した場合の、既存CSV系一括登録機能（`Admin.tsx`内）の要否をユーザーと整理
  - **「調整さん 出欠一括登録（CSV）」：不要（削除候補）**。外部の調整さんで集めた出欠を後から取り込むための機能であり、新しい調整機能（アプリ内で直接回答を集める）が代替するため
  - **「正規部員 一括登録（CSV）」：存続**。出欠収集ではなく、複数の正規部員アカウントをまとめて新規作成するための機能であり、調整機能とは目的が異なるため
  - **「ゲスト 一括登録（CSV）」：不要（削除候補）**。今後ゲストは調整機能経由で個別にID発行される想定のため
- 上記3件はいずれも設計段階の整理であり、削除・改修はまだ実施していない（対象コードは`Admin.tsx`に現存）

## 2026-09-24 ホーム画面の経験者/未経験者カウント不具合を修正

- 日程調整機能のリリース後、本番環境で正規部員＋ゲストが同時に参加するデータが初めてでき、ホーム画面「今日の参加状況」の経験者/未経験者数が、メンバー画面の集計結果と食い違う不具合が表面化
  - ホーム画面：0経験者/2未経験者、メンバー画面：1経験者/1未経験者（実データは正規部員1名（経験者）＋ゲスト1名（未経験者））
- 原因：`Home.tsx`の集計がゲストの一時レベル（`memberLevels`）のみを参照しており、正規部員の永続レベル（`users`側の`level`、`dbLevels`として保持）を見ていなかった。`Matches.tsx`・`Members.tsx`は既に正しく両方を参照していた
- `git log`確認の結果、この不具合は2026-05-24時点（`874b47e`）から存在しており、今回の日程調整機能リリースとは無関係な既存バグと判明
- 修正方針：`Matches.tsx`で既に使われている「呼び出し側で`{ ...memberLevels, ...dbLevels }`と合成してから渡す」方式を採用し、`App.tsx`の`<Home>`呼び出し1箇所のみを修正（`Home.tsx`自体は無修正）。`Members.tsx`方式（`isMember`判定を自前で持つ）より変更範囲が小さく、既存の合成パターンとの一貫性もあるため採用
- 本番データでゲストログインして確認し、ホーム画面が「1経験者/1未経験者」に修正されたことを確認

## 2026-09-24 動作速度の懸念に対する調査と①の修正（日程確定処理の並列化）

- 「今は遅くないが、10〜30人規模で使ったときに遅くなって使えなくなるのが心配」という相談を受け、本番Firebase（読み取り専用REST）に対する実測を行い、人数増加で悪化する箇所を2件特定
  - **①（今回修正）**：[AdminSchedule.tsx](../src/components/AdminSchedule.tsx)の`handleConfirmDate`（管理者が候補日を確定するボタン）が、仮登録ゲストIDのロック解除のためのFirebase問い合わせを**1人ずつ順番に**（`for`内で`await`）行っていた。実測した1回あたりの往復時間（約0.25秒）から、30人規模では確定ボタンの処理に約7.7秒かかる見込みと算出
  - **②（未対応）**：[useFirebase.ts](../src/hooks/useFirebase.ts)が`badminton`以下の全データを1つの監視（`onValue`）でまとめて見ており、誰か1人の操作のたびに全接続者へ全データが再送信される作り。現状のデータ量（1,203バイト）では体感できないが、同時多数アクセス時にじわじわ効いてくる可能性があり、対応するならアプリ全体の設計に関わる規模の変更になるため、着手は別途相談
- ユーザーから「①を直した後に②を直すと、①がまた手戻りになるか」との質問があり、①は1回限りの管理者操作（`handleConfirmDate`関数内で完結）、②は常時接続のリアルタイム監視の仕組みで、性質が異なり通常は競合しないと回答（`AdminSchedule.tsx`という同じファイルは②対応時に触る可能性はあるが、①で直した箇所とは別部分）
- ①の修正内容：「回答者ごとに1件ずつ問い合わせて待つ」ループを、対象者の`linkedGuestId`を先にすべて集めてから`Promise.all`で同時に問い合わせる方式に変更。結果を使っていない空の`else if`分岐（死んだコード）も同時に削除
- 型チェック・ビルドとも正常。管理者操作（実データを書き換える）のため、本番での実行テストは行わず、コードの検証のみで確認

## 2026-09-24 本番Firebaseにセキュリティルールをデプロイ

- 「usersが誰でも読める」「書き込み制限が全くない」という問題への緊急対応として、無差別な自動攻撃（荒らし・削除ボット対策）を目的に、`database.rules.json`を作成しFirebase CLI経由でデプロイ
- 読み取りは引き続き全開放（現状のアプリ構造上、ログイン判定自体がusers全体の読み取りを必要とするため変更不可）。書き込みは、アプリが実際に書き込む形（フィールド構成・型・enum値）と一致しないデータを拒否する形式検証を追加
- 本番URLで実際に書き込み操作（ゲストID発行→パスワード設定）を行い、拒否されず成功することを確認。テストデータは削除済み
- **この対策の限界**：形式が正しければ書き込めてしまうため、アプリの構造を理解した人間による意図的な攻撃は防げない。根本解決には認証方式の見直し（後述）が必要

## 2026-09-24 本質的な問題の特定：認証を自作していること

- ユーザーからの「本質的な問題はどこにあるか」という問いに対し、これまでの指摘（usersが読める・書き込み制限が作れない・初期パスワードの使い回しが危険）はすべて**Firebase公式のログイン機能（Firebase Authentication）を使わず、自前でID・パスワード照合を実装していること**という1つの根本原因から派生した症状であると整理
- 自作ログインだとFirebaseのルールが「本人かどうか」を判定できないため、正規のセキュリティルールが原理的に書けない。usersの全体公開もこの制約から生じている
- **結論：④（認証方式の見直し）が唯一の本質的解決策であり、他の対症療法より優先すべき**という合意に至った

## 2026-09-24 ④認証方式の見直し（Firebase Authenticationへの移行）に着手

- ユーザーから「④を完了まで進めてください。ただしPushまでは進めないでください」との指示を受け、実装に着手
- **設計**：このアプリのID（例：`ab3x9k2m`）はメール形式ではないため、`{id}@badminton.local`というダミードメイン付きメールアドレスに変換してFirebase Authentication（メール/パスワード方式）に登録する。UI・ログイン操作感は変更なし（ID・パスワードを入力する画面のまま）
- **実装した変更**：
  - `firebase.ts`：Firebase Authenticationの初期化、ID→メール変換関数を追加
  - `auth.ts`：`loginWithId`をFirebase Authenticationでの照合に置き換え。`createAuthAccount`（新規アカウント作成）、`changeOwnPassword`（本人による変更、現在PWの再認証あり）、`setOwnPassword`（初回ログイン直後の強制変更）を追加
  - `Login.tsx`（初回セットアップ）・`GuestPasswordSetup.tsx`（ゲスト自己登録）・`ScheduleForm.tsx`（出欠回答フォーム経由のゲスト自動登録）・`Admin.tsx`（新規ID発行・CSV一括登録）：アカウント作成箇所すべてに`createAuthAccount`を追加
  - `PasswordChange.tsx`・`SettingsModal.tsx`：本人によるパスワード変更をFirebase Authentication経由に変更
- **設計上の注意点として気づき対応したこと**：
  - Firebaseの仕様上、アカウント作成はその場で「作成したアカウントへのログイン」を兼ねてしまうため、管理者が他人のアカウントを作ると管理者自身のログインが切れてしまう問題を発見。使い捨ての別インスタンス（一時的なFirebaseアプリ）上でアカウント作成する方式に変更し回避
  - パスワードの最低文字数について、Firebase Authenticationの既定要件（6文字以上）と、このアプリの一部画面（ゲストの初期パスワード設定）で使っていた「4文字以上」の表記が食い違っていたため、6文字以上に統一
- **既存機能で対応不能と判明した点**：管理者が「他人の」パスワードを直接リセットする機能（`handleResetPassword`）・「他人の」アカウントを削除する機能は、Firebase Authenticationの仕様上クライアント側から行えない（サーバー側の管理者権限＝Admin SDKが必要）
  - パスワード初期化：「今のIDを無効化し、同じ名前・権限で新しいIDを発行する」という代替方式に変更（本人には新しいIDとパスワードを再度伝える必要がある、という運用変更を伴う）
  - アカウント削除：`users`側のレコードを消せばログイン後の照合が必ず失敗するため、Firebase Authentication側のアカウントが残っていても実害はなく、対応不要と判断
- **ブロッカー（ユーザー対応待ち）**：
  1. Firebaseコンソールでの「メール/パスワード」ログイン方式の有効化（Firebase Authentication自体の初期化は完了したが、方式ごとの個別トグルがまだ無効な状態を確認）
  2. 既存の登録者（正規部員・ゲスト）をFirebase Authenticationへ移行する一度きりのスクリプト（`scripts/migrate-existing-accounts.mjs`として準備済み、未実行）のために`firebase-admin`パッケージを開発用にインストールする許可
- 型チェック・ビルドは正常。上記ブロッカーにより、実際のログイン動作の本番確認と移行はまだ実施できていない。指示通りpushはしていない

## 2026-09-24 ④の本番反映・既存アカウント移行の完了、新たな設計課題の発見

- ユーザーがFirebaseコンソールでメール/パスワード方式を有効化、`firebase-admin`のインストールも許可
- `scripts/migrate-existing-accounts.mjs`を実行し、既存6アカウント（owner1・admin4・member1）をFirebase Authenticationへ移行（初期パスワード`nicesoul`・`isFirstLogin: true`）
- ローカルdevサーバーで新規アカウント作成→ログインの一連の流れを本番Firebaseに対して実地テストし成功を確認。テストデータは削除
- コミット・push（`ed6523c`）→ GitHub Actionsのデプロイ成功を確認。本番URLでも正常動作を確認
- **ユーザーから「isFirstLoginがfalseなら今までのパスワードで入れるか」との質問があり、誤解を訂正**：`isFirstLogin`はログイン後に強制的にパスワード変更画面を出すかどうかのフラグに過ぎず、ログイン自体の可否には影響しない。移行によりFirebase Authentication側のパスワードは`nicesoul`に統一されているため、true/falseに関わらず今までの実際のパスワードではもうログインできない
- **テストユーザー削除の際、新たな設計課題を発見**：クライアント側で作成するアカウント（`createAuthAccount`経由）は、Firebase Authenticationの内部ID（uid）をこちらで指定できず、Firebase側がランダムに自動採番する。移行スクリプトは`uid`をアプリのIDに強制していたため、**移行済みアカウントと今後新規作成されるアカウントとでuidの扱いが不統一**になっていることが判明（ログイン自体には支障ないが、将来の本格的なアクセス制限ルール設計に影響する）
- ユーザーへ対応方針を確認：「①対応表を追加するだけで済ませる案」と「②実データも含めて作り直す案」を提示し、**②（作り直す）を選択**（実際の利用がまだ始まっていないため）
  - `database.rules.json`に`authUidToId`（uid→アプリのID対応表）ノードを追加しデプロイ
  - `auth.ts`の`createAuthAccount`を、アカウント作成のたびにこの対応表へ書き込むよう修正
  - `scripts/migrate-existing-accounts.mjs`を、uidをアプリのIDに強制する方式から、Firebase側の自動採番＋対応表書き込みという、クライアント側と統一された方式に書き直し（実行待ち：秘密鍵ファイルの再発行待ち、ユーザーが削除を指示したため）
- 型チェック・ビルドは正常

## 2026-09-24 ③全データ監視の設計見直し（useFirebase.tsのスコープ分割）

- 以前から積み残しだった性能課題に着手：`useFirebase.ts`が`onValue(ref(db, ROOT))`で`badminton`以下を丸ごと1つの監視でまとめて見ており、`users`・`chouseisanEntries`・`candidateDates`等、アプリの状態（`AppState`）に無関係なデータが変わるたびにも、全接続者へ全データが再送信・再計算される作りだった
- `AppState`が実際に使う8つのパス（`attendance`・`memberLevels`・`activityDate`・`courts`・`matches`・`party`・`paymentClub`・`paymentParty`）それぞれに個別の`onValue`を張る方式に分割。無関係なパス（`users`・`guestUsers`・`chouseisanEntries`・`candidateDates`・`authUidToId`）への書き込みは、もうこのフックの再計算を一切引き起こさなくなった
- `loading`の完了判定は、8パスすべてから最低1回ずつスナップショットを受け取るまで待つ方式（`loadedPaths`セットで管理）に変更
- 活動日終了後・7日後の自動リセット処理は、`activityDate`監視のコールバック内に移動。以前はリセット対象の全フィールドをその場でローカル変数として空にしていたが、新方式では各パスの監視が実際の書き込み結果を受け取ってから反映される（体感できない程度の遅延はあるが、常にサーバーの実データと一致するという点でより正確な動作になった）
- ローカルdevサーバーで本番Firebaseに対して動作確認（ホーム画面の出欠集計・メンバー一覧の支払い状況等）を行い、変更前と同じ表示になることを確認。コンソールエラーなし
- 型チェック・ビルドは正常

## 2026-09-24 ゲスト初期パスワードの変更、プライバシーポリシーの追加

- ゲスト自己登録・出欠回答フォーム経由の自動登録で使う初期パスワードを、ユーザーの希望により`nicesoul`から`boldbad`に変更（`ScheduleForm.tsx`のみ。正規部員向け`Admin.tsx`側は`nicesoul`のまま）。Firebase Authenticationの最低文字数要件（6文字以上）を満たすことを確認した上で変更
- 「ID無効化」の仕組み（Firebase Authentication側のアカウントは残るが、RTDB側のレコードを消すことでログイン判定が必ず失敗する、という二段階チェックの片方だけを崩す方式）をユーザーに説明
- 個人情報保護方針について、ユーザーから「必要であれば対応してほしい」との依頼を受け、`PrivacyPolicy.tsx`を新規作成しログイン画面下部からモーダルで開けるよう実装。取得する情報・利用目的・第三者提供の有無・保存期間・開示等の依頼先を明記。実際に画面表示を確認済み
- 型チェック・ビルドは正常

## 2026-09-24 管理タブに「ゲスト一覧」を追加

- ユーザーから「登録済みの人へIDを伝えたいが、IDが分からないと本人はログインできず、一覧を見ることもできない」という、正規部員一覧にはあってゲストには無かった機能ギャップの指摘を受け、対応
- `Admin.tsx`に「ゲスト一覧」セクションを新設。`guestUsers`をリアルタイム監視し、名前・ID・状態（日程確定待ちでログイン不可／利用可能）を一覧表示し、IDコピー・削除操作もできるようにした（既存の「正規部員一覧」と同じ表構成・操作感で統一）
- 型チェック・ビルドは正常。ただし管理者ログイン情報を持っていないため、画面表示自体はブラウザで未確認（本番反映後にユーザー側での確認を依頼）
- ユーザーが自身のID（今まで通りのID・パスワードは`nicesoul`）でログインし、上記の変更内容を確認できたことを確認

## 2026-09-24 日程調整：候補日をカレンダーからまとめて複数選択できるように変更

- ユーザーから「複数日付の候補日追加を複数一回で選択できるようにできないか」との要望を受け対応
- 従来は`<input type="date">`で1件ずつ日付を選んで「候補日を追加」ボタンを押す方式だった（HTML標準の日付入力は複数選択に対応していないため）
- `AdminSchedule.tsx`に月表示のカレンダーグリッドを自作し、複数の日をクリックしてまとめて選択→「選択した候補日を追加する（N件）」で一括登録する方式に変更。前月・次月の切り替えも可能。既に候補日として登録済みの日はグレーアウトして選択不可にした
- 型チェック・ビルドは正常。この機能も管理タブ内のため画面表示は未確認

## 2026-09-24 Claude確認専用テストアカウントの仕組みを整備

- 「Claude専用の全画面確認用IDを作れるか」との相談を受け、以下の方針で対応
  - `UserRecord`に`hidden?: boolean`を追加し、管理画面の「正規部員一覧」・人数カウント（owner/admin表示）から除外できるようにした（`database.rules.json`にも対応するバリデーションを追加）
  - `Admin.tsx`の`fetchUsers`で`hidden: true`のレコードを一覧生成時点でフィルタし、一覧にもカウントにも一切表示されないようにした
  - テストアカウントのID・パスワードは`docs/local_test_account.md`というファイルに保存し、`.gitignore`に追加してGitHubには絶対に含まれないようにした（ローカルのこのパソコンにのみ存在）
- 型チェック・ビルドは正常
